name: pythonsampleproject
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: pythonsampleproject
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
